/**
 * OrderService 常量定义
 */

import { BASE_API } from '../../config/api';

// API 基础 URL
export const API_BASE_URL = BASE_API;

// 轮询间隔 (毫秒)
export const POLLING_INTERVAL = 5000;

// 存储键名
export const NETWORK_ORDERS_KEY = '@network_orders';
export const TCP_ORDERS_KEY = '@tcp_orders'; 